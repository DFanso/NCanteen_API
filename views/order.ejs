<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order <%= order._id %></title>
    <style>
        /* Google Fonts */
        @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap');

        /* Basic reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Set the main font for the page */
        body {
            font-family: 'Roboto', sans-serif;
            line-height: 1.6;
            background-color: #f5f5f5;
            color: #333;
        }

        /* Container */
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0px 3px 15px rgba(0, 0, 0, 0.1);
        }

        /* Headers */
        h1, h2, h3 {
            margin-bottom: 15px;
        }

        /* List styles */
        ul {
            list-style-type: none;
            margin-bottom: 20px;
        }

        li {
            margin-bottom: 10px;
        }

    </style>
</head>
<body>
    <div class="container">
        <h1>Order Details</h1>
        <h2>Order ID: <%= order._id %></h2>
        <h3>Status: <%= order.status %></h3>
        <h3>Price: Rs.<%= order.price.toFixed(2) %></h3>
        <h3>Items:</h3>
        <ul>
            <% order.items.forEach(item => { %>
                <li><%= item.name %> x<%= item.quantity %> - Rs.<%= item.price.toFixed(2) %></li>
            <% }); %>
        </ul>
    </div>
</body>
</html>
